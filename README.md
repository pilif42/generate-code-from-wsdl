This project shows how to generate client code from a wsdl.

The only thing to do is to copy your wsdl(s) under resources\wsdls. And then, build the project.


# To build the project
mvn clean install

